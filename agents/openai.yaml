interface:
  display_name: "Copilot BYOK Assistant"
  short_description: "Use Copilot CLI for structured BYOK reviews."
  default_prompt: "Use $copilot-byok-assistant to run a structured Copilot review through my configured custom provider."
